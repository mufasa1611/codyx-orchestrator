{
  "name": "@cody/android",
  "private": true,
  "version": "1.14.41",
  "type": "module",
  "scripts": {
    "version": "bun scripts/sync-version.ts",
    "update": "bun run version && bunx @bubblewrap/cli update --manifest twa-manifest.json",
    "build": "bun run update && bunx @bubblewrap/cli build --manifest twa-manifest.json --skipPwaValidation",
    "build:unsigned": "bun run update && bunx @bubblewrap/cli build --manifest twa-manifest.json --skipPwaValidation --skipSigning"
  },
  "license": "MIT"
}
